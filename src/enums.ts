import { CANCELLED } from "dns";

export enum UserRoleEnum {
  USER = "USER",
  ADMIN = "ADMIN",
}
export enum FoodOrderStatusEnum {
  PENDIND = "PENDING",
  CANCELLED = "CANCELED",
  DELIVER = "DELIVERED",
}
